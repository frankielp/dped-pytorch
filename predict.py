import os
import sys

import imageio
import numpy as np
import torch
from torch import nn

import utils.parse_arg


def run(phone, dped_dir, test_subset, iteration, resolution, use_gpu):
    pass


if __name__ == "__main__":
    run(**utils.parse_arg.test_args(sys.argv))
